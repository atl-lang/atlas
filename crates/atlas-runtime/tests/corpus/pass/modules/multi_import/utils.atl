// utils.atlas - multiple exports
export fn double(borrow x: number) -> number {
    return x * 2;
}

export fn triple(borrow x: number) -> number {
    return x * 3;
}

export let SCALE: number = 100;

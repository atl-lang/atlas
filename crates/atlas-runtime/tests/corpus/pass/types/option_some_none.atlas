let opt_some: Option<number> = Some(42);
let opt_none: Option<number> = None();

console.log(is_some(opt_some));
console.log(is_none(opt_some));
console.log(is_some(opt_none));
console.log(is_none(opt_none));

let val: number = unwrap(opt_some);
console.log(val);

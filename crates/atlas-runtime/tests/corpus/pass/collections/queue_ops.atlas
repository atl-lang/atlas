let q = new Queue<string>();
q = q.enqueue("first");
q = q.enqueue("second");
q = q.enqueue("third");

console.log(q.size());

let item: string = unwrap(q.dequeue());
console.log(item);
console.log(q.size());

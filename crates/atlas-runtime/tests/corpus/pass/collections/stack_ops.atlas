let s = new Stack<number>();
s = s.push(10);
s = s.push(20);
s = s.push(30);

console.log(s.size());

let top: number = unwrap(s.peek());
console.log(top);

let popped: number = unwrap(s.pop());
console.log(popped);
console.log(s.size());

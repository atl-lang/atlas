let hm = hashMapNew();
hashMapPut(hm, "name", "Alice");
hashMapPut(hm, "age", "30");
hashMapPut(hm, "city", "Paris");

console.log(hashMapSize(hm));
console.log(hashMapHas(hm, "name"));
console.log(hashMapHas(hm, "missing"));

let val: string = unwrap(hashMapGet(hm, "name"));
console.log(val);

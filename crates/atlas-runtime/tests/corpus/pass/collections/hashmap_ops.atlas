let hm = new Map<string, string>();
hm = hm.set("name", "Alice");
hm = hm.set("age", "30");
hm = hm.set("city", "Paris");

console.log(hm.size());
console.log(hm.has("name"));
console.log(hm.has("missing"));

let val: string = unwrap(hm.get("name"));
console.log(val);

let hm = Map();
hm.set("name", "Alice");
hm.set("age", "30");
hm.set("city", "Paris");

console.log(hm.size());
console.log(hm.has("name"));
console.log(hm.has("missing"));

let val: string = unwrap(hm.get("name"));
console.log(val);

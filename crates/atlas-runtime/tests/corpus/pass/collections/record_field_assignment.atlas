let mut cfg = record {
  host: "localhost",
  port: 8080,
  nested: record { port: 1 }
};

cfg.port = 9000;
cfg.host = "0.0.0.0";
cfg.nested.port = 7000;

console.log(cfg.host);
console.log(cfg.port);
let nested = cfg.nested;
console.log(nested.port);

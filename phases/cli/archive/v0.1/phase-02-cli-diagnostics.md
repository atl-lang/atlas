# Phase 02 - CLI Diagnostics

## Objective
Implement CLI diagnostics handling, including JSON output.

## Inputs
- `docs/diagnostics.md`

## Deliverables
- `--json` flag behavior.
- Non-zero exit codes for errors.

## Steps
- Add `--json` flag to CLI.
- Ensure diagnostics are routed correctly.

## Exit Criteria
- CLI integration tests validate output formats.

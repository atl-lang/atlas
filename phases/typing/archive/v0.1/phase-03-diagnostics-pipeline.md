# Phase 03 - Diagnostics Pipeline

## Objective
Implement the unified `Diagnostic` type and formatting logic.

## Inputs
- `docs/diagnostics.md`

## Deliverables
- `Diagnostic` struct with fields from `docs/diagnostics.md`.
- Human and JSON renderers.
- Error code registry.
- Support for secondary/related spans.

## Steps
- Create diagnostic structs and helpers.
- Implement JSON output with stable ordering.
- Add unit tests for format outputs.

## Exit Criteria
- Diagnostic unit tests for human and JSON output.

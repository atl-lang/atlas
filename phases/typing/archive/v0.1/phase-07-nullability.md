# Phase 07 - Nullability Rules

## Objective
Enforce explicit null rules in the type checker.

## Inputs
- `Atlas-SPEC.md`

## Deliverables
- Tests for rejecting implicit null assignment.
- Tests for `null` type usage.

## Steps
- Add tests for null assignment to non-null types.
- Add tests for explicit `null` variable usage.

## Exit Criteria
- Nullability rules enforced with correct error codes.

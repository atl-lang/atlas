# Phase 06 - Control Flow

## Objective
Implement `if`, `while`, `for`, `break`, and `continue` semantics.

## Inputs
- `Atlas-SPEC.md`

## Deliverables
- Control flow execution in interpreter.
- Correct handling of `break`/`continue` in loops.

## Steps
- Implement `if` and `while` execution.
- Add `for` loop lowering or direct evaluation.

## Exit Criteria
- Control flow tests pass.

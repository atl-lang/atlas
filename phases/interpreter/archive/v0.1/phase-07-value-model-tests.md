# Phase 07 - Value Model Tests

## Objective
Verify value representation semantics and equality rules.

## Inputs
- `docs/RUNTIME.md`

## Deliverables
- Tests for number, string, bool, null equality.
- Tests for array reference equality.
- Tests for mutation visibility across references.

## Steps
- Build a targeted test suite for equality and mutability.
- Ensure tests run in interpreter and VM.

## Exit Criteria
- Value model tests pass in interpreter and VM.

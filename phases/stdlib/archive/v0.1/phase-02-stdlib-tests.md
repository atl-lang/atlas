# Phase 02 - Stdlib Tests

## Objective
Create deterministic tests for stdlib behavior.

## Inputs
- `Atlas-SPEC.md` (for correct Atlas syntax in test fixtures)
- `docs/api/stdlib.md`

## Deliverables
- Golden tests for `print`, `len`, `str`.
- Error cases for invalid arguments.

## Steps
- Create golden test fixtures.
- Validate outputs and diagnostics.

## Exit Criteria
- Stdlib tests pass in interpreter and VM.

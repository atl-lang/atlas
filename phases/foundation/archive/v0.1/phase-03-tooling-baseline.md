# Phase 03 - Tooling Baseline

## Objective
Set up baseline tooling for linting, formatting, and dependency checks.

## Inputs
- `docs/guides/code-quality-standards.md`
- `docs/guides/code-quality-standards.md`

## Deliverables
- `rustfmt` config (if needed).
- Optional `clippy` configuration notes.
- Dependency audit instructions (`cargo audit`, `cargo deny`).

## Steps
- Decide whether a custom `rustfmt.toml` is needed.
- Document clippy expectations in README or CONTRIBUTING.
- Add security tooling notes in README.

## Exit Criteria
- Tooling instructions documented in README.

# Phase 10 - Runtime API Tests

## Objective
Add tests for the runtime API behavior.

## Inputs
- `archive/test-plans/runtime-api-test-plan.md` (archived test plan)

## Deliverables
- Unit tests for `eval` and `eval_file`.
- Tests for diagnostic errors on invalid input.

## Steps
- Implement test fixtures.
- Ensure tests do not require CLI.

## Exit Criteria
- Runtime API tests pass consistently.

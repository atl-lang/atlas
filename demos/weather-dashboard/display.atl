// Display module - Beautiful CLI output formatting
import { repeat, padEnd, padStart } from "std:string";
import { floor } from "std:math";

// Create horizontal line
fn line(width: number, char: string) -> string {
    return repeat(char, width);
}

// Display dashboard header
export fn displayHeader() -> void {
    print("╔═══════════════════════════════════════════════════════╗");
    print("║           Weather Dashboard - Live Data              ║");
    print("╚═══════════════════════════════════════════════════════╝");
    print("");
}

// Display city weather card
export fn displayCityWeather(
    location: string,
    temp: number,
    feelsLike: number,
    description: string,
    emoji: string,
    humidity: number,
    wind: number,
    windDir: string,
    useMetric: bool
) -> void {
    let tempUnit: string = if (useMetric) { "°C" } else { "°F" };

    // Header
    print("┌────────────────────────────────────────────────┐");
    print("│  " + location);
    print("├────────────────────────────────────────────────┤");

    // Main weather
    let tempStr: string = toString(floor(temp)) + tempUnit + "  " + emoji + "  " + description;
    print("│  " + tempStr);

    // Feels like
    let feelsStr: string = "Feels like " + toString(floor(feelsLike)) + tempUnit;
    print("│  " + feelsStr);

    print("├────────────────────────────────────────────────┤");

    // Details
    print("│  Humidity: " + toString(humidity) + "%");
    print("│  Wind: " + toString(wind) + " km/h " + windDir);

    print("└────────────────────────────────────────────────┘");
    print("");
}

// Display error for a city
export fn displayCityError(city: string, error: string) -> void {
    print("┌────────────────────────────────────────────────┐");
    print("│  " + city);
    print("├────────────────────────────────────────────────┤");
    print("│  ❌ Error: " + error);
    print("└────────────────────────────────────────────────┘");
    print("");
}

// Display loading message
export fn displayLoading(city: string) -> void {
    print("⏳ Fetching weather for " + city + "...");
}

// Display summary statistics
export fn displaySummary(totalCities: number, successful: number, failed: number, useMetric: bool) -> void {
    print("───────────────────────────────────────────────────────");
    print("Summary: " + toString(successful) + "/" + toString(totalCities) + " cities loaded successfully");

    if (failed > 0) {
        print("Failed: " + toString(failed) + " cities");
    }

    let unit: string = if (useMetric) { "Celsius" } else { "Fahrenheit" };
    print("Temperature unit: " + unit);

    print("Cache: Data cached for 30 minutes");
    print("───────────────────────────────────────────────────────");
}

// Display help text
export fn displayHelp() -> void {
    print("");
    print("💡 Tips:");
    print("  • Edit config/cities.json to add/remove cities");
    print("  • Weather data is cached for 30 minutes");
    print("  • Uses wttr.in public API (no API key needed)");
    print("");
}

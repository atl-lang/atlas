[project]
name = "web-crawler"
version = "0.1.0"
description = "Web crawler demonstrating HTTP, Regex, Collections (HashSet, Queue, HashMap), File I/O, and JSON export"

[permissions]
network = ["*"]
filesystem = ["./output/*", "./config/*"]

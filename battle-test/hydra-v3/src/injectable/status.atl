export fn build_status_response(
    server_name: string,
    snapshot: { state: string, pid: number, uptime_seconds: number, restarts_in_window: number, last_restart_reason: string, last_error: string, queue_size: number, can_recover: bool },
    behavior: { max_restarts: number, restart_window_seconds: number }
) -> json {
    let payload = record {
        state: snapshot.state,
        server_name: server_name,
        pid: snapshot.pid,
        uptime_seconds: snapshot.uptime_seconds,
        restarts_in_window: snapshot.restarts_in_window,
        max_restarts: behavior.max_restarts,
        restart_window_seconds: behavior.restart_window_seconds,
        last_restart_reason: snapshot.last_restart_reason,
        last_error: snapshot.last_error,
        queue_size: snapshot.queue_size,
        can_recover: snapshot.can_recover
    };
    return toJSON(payload);
}

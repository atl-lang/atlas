// Recording hooks (Atlas port)
// NOTE: Recorder API not implemented; this is a no-op wrapper.

export fn record(borrow source: string, borrow payload: string, borrow is_client: bool) -> void {
    return;
}

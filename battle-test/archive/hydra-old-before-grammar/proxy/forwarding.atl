// Forwarding helpers (Atlas port)
// NOTE: No transport write interfaces; no-op placeholders.

export fn forward_to_child(borrow payload: string) -> void { return; }
export fn forward_to_client(borrow payload: string) -> void { return; }

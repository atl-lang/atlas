// Restart tool (Atlas port)

export fn hydra_restart(borrow reason: string) -> bool {
    // No supervisor hook
    return true;
}

export fn hydra_force_restart(borrow confirm: bool) -> bool {
    if (!confirm) { return false; }
    return true;
}

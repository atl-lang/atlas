// Redactor (Atlas port)

export fn new_redactor(borrow replacement: string) -> json {
    let rep = replacement;
    if (len(rep) == 0) { rep = "[REDACTED]"; }
    let obj = { replacement: rep };
    let encoded = toJSON(obj);
    let parsed = parseJSON(encoded)?;
    return parsed;
}

export fn redact(borrow redactor: json, borrow content: string, borrow patterns: array) -> string {
    let result = content;
    for pattern in patterns {
        let re = regexNew(pattern)?;
        result = regexReplaceAll(re, result, redactor["replacement"].as_string());
    }
    return result;
}

// Supervisor process (Atlas port, simplified)
// Source: hydra/internal/supervisor/process.go

import { execute_command } from "./supervisor";

export fn start_process(borrow cmd: array, borrow cwd: string) -> Result<json, string> {
    // No async process; exec synchronously
    let out = execute_command(cmd)?;
    return Ok(out);
}

export fn stop_process(borrow pid: number) -> Result<null, string> {
    // No process control; no-op
    return Ok(null);
}

export fn uptime(borrow start_ms: number, borrow now_ms: number) -> number {
    return now_ms - start_ms;
}

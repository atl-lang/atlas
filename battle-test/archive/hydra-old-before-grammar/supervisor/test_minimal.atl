// Minimal test to debug syntax

fn main() -> void {
    print("=== Minimal Test ===");

    // Test 1: Create object
    let obj: object = {
        state: 0,
        name: "test"
    };

    print("Object created");

    // Test 2: Access object
    let s: number = obj["state"];
    print("State: " + str(s));

    print("=== Done ===");
}

main();

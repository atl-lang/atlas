// Sanitizer mock (Atlas port, simplified)
// Source: hydra/internal/mocks/Sanitizer.go

import { classify_chunk } from "../sanitizer/classifier";

export fn classify(borrow chunk: string) -> string {
    return classify_chunk(chunk);
}

export fn validate_utf8(borrow data: string) -> string {
    return data;
}

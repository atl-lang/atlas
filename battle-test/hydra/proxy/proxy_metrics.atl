// Proxy metrics (Atlas port)
// NOTE: No metrics backend; no-op functions.

export fn record_request_complete(start_ms: number, success: bool) -> void { return; }
export fn record_queue_wait(enqueue_ms: number) -> void { return; }
export fn record_restart(reason: string) -> void { return; }

// Approximate pi/4 using Leibniz formula
// pi/4 = 1 - 1/3 + 1/5 - 1/7 + ...
let sum = 0;
let sign = 1;
let i = 0;
while (i < 10000) {
    sum = sum + sign / (2 * i + 1);
    sign = -sign;
    i = i + 1;
}
sum;
